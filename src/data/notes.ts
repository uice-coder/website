/** Presenter scripts. Roughly 25–35 seconds each at a calm speaking pace. */
export const PRESENTER_NOTES: Record<string, string> = {
  'closed-book':
    'Here is a simple question about the human ear. BART answers it from memory alone — no books, no search. Notice the answer sounds fluent, but it repeats itself and mentions the nose, which is wrong. This is the real example from the paper. The problem is not only the error. It is that we cannot see where the answer came from, and we cannot easily correct it. So — what if the model could look something up first?',
  'how-rag-works':
    'Same question, different method. The question becomes a numeric query. DPR searches a Wikipedia index of about twenty-one million short passages and scores each one. The irrelevant passages drop away, and the top three go to the generator, together with the question. BART now writes an answer using that evidence — and we can hover any part of it to see the passage behind it. Notice the answer is not copied from one passage: it combines two. Three steps: search, read, generate. But where is the knowledge actually stored?',
  'two-memories':
    'RAG uses two kinds of memory. On the left, parametric memory: facts baked into BART’s weights during training. Powerful, but sealed — you cannot open it or edit it. On the right, non-parametric memory: an external index you can search, read, and simply replace when the world changes. DPR connects the question to that library. Brain alone gives fluency without evidence. Library alone gives evidence without an answer. Together, you get both. Now — how does the model actually use those documents?',
  'sequence-vs-token':
    'The paper gives two versions. RAG-Sequence asks: which document supports this whole answer? It writes a full answer for each document, scores each route, then combines them — nothing is thrown away. RAG-Token asks a narrower question: which document supports this word, right now? Watch the Hemingway example: "The Sun Also Rises" comes mostly from document two, "A Farewell to Arms" from document one. One generation, two sources, blended word by word. So — does any of this actually work?',
  findings:
    'Does it work? On Natural Questions, the parametric-only T5 scores 34.5. DPR, which retrieves and extracts, gets 41.5. Both RAG models pass 44. Then human evaluators compared RAG-Token and BART on Jeopardy questions: RAG was judged more factual 42.7 per cent of the time, BART only 7.1 per cent. Note carefully — this is a human preference judgement, not a hallucination count. And RAG worked across four different task types, which is the paper’s real claim: a general recipe. But does that mean the problem is solved?',
  limits:
    'One honest question: does RAG solve hallucination? No. Three things can still fail — retrieval brings the wrong passage; the source itself is outdated; or the generator states something the evidence never said. Watch what happens when I rank misleading evidence first — the answer follows it. Relevant is not the same as true. So the takeaway is: search, read, generate. RAG makes answers more grounded and checkable than memory alone — it does not make them guaranteed.',
};
